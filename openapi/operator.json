{
  "openapi": "3.1.0",
  "info": {
    "title": "kestrel operator",
    "version": "0.1.0",
    "summary": "The boundary a Client reaches a control plane over.",
    "description": "Server-sent events down, POST up, plain HTTP. It authenticates nobody, so the control plane serves it on a listener of its own, apart from the link, and binds that listener to loopback unless told otherwise. A remote control plane is reached through a tunnel.",
    "license": {
      "name": "Apache-2.0",
      "identifier": "Apache-2.0"
    }
  },
  "servers": [
    {
      "url": "{controlPlane}",
      "variables": {
        "controlPlane": {
          "default": "http://127.0.0.1:7718"
        }
      }
    }
  ],
  "security": [],
  "paths": {
    "/operator/organizations": {
      "get": {
        "operationId": "listOrganizations",
        "summary": "List every Organization.",
        "responses": {
          "200": {
            "description": "Every record, ordered by name.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/Organization"
                  }
                }
              }
            }
          },
          "503": {
            "$ref": "#/components/responses/Refused"
          }
        }
      },
      "post": {
        "operationId": "declareOrganization",
        "summary": "Declare an Organization.",
        "description": "Declaring an Organization that already exists changes nothing and answers the one that exists.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/OrganizationDeclaration"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "A record by this name already existed, and is now what the declaration describes.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Organization"
                }
              }
            }
          },
          "201": {
            "description": "No record by this name existed, and one was made.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Organization"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/Refused"
          },
          "422": {
            "$ref": "#/components/responses/Refused"
          },
          "503": {
            "$ref": "#/components/responses/Refused"
          }
        }
      }
    },
    "/operator/organizations/{organization}/workspaces": {
      "get": {
        "operationId": "listWorkspaces",
        "summary": "List every Workspace in an Organization.",
        "responses": {
          "200": {
            "description": "Every record, ordered by name.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/Workspace"
                  }
                }
              }
            }
          },
          "404": {
            "$ref": "#/components/responses/Refused"
          },
          "503": {
            "$ref": "#/components/responses/Refused"
          }
        },
        "parameters": [
          {
            "name": "organization",
            "in": "path",
            "required": true,
            "description": "The name of the Organization the records belong to.",
            "schema": {
              "type": "string"
            }
          }
        ]
      },
      "post": {
        "operationId": "declareWorkspace",
        "summary": "Declare a Workspace.",
        "description": "Desired state: a Workspace by this name that already exists keeps its identity and takes on the repositories and branch declared.",
        "parameters": [
          {
            "name": "organization",
            "in": "path",
            "required": true,
            "description": "The name of the Organization the records belong to.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WorkspaceDeclaration"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "A record by this name already existed, and is now what the declaration describes.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Workspace"
                }
              }
            }
          },
          "201": {
            "description": "No record by this name existed, and one was made.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Workspace"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/Refused"
          },
          "404": {
            "$ref": "#/components/responses/Refused"
          },
          "422": {
            "$ref": "#/components/responses/Refused"
          },
          "503": {
            "$ref": "#/components/responses/Refused"
          }
        }
      }
    },
    "/operator/organizations/{organization}/agents": {
      "get": {
        "operationId": "listAgents",
        "summary": "List every Agent in an Organization.",
        "responses": {
          "200": {
            "description": "Every record, ordered by name.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/Agent"
                  }
                }
              }
            }
          },
          "404": {
            "$ref": "#/components/responses/Refused"
          },
          "503": {
            "$ref": "#/components/responses/Refused"
          }
        },
        "parameters": [
          {
            "name": "organization",
            "in": "path",
            "required": true,
            "description": "The name of the Organization the records belong to.",
            "schema": {
              "type": "string"
            }
          }
        ]
      },
      "post": {
        "operationId": "declareAgent",
        "summary": "Declare an Agent.",
        "description": "Desired state: an Agent by this name that already exists keeps its identity and takes on the runtime and model declared, so a declaration naming no model leaves it naming none. A model its runtime has been seen to advertise, and this is not among them, is refused with 422. A Run already in flight keeps the model it was dispatched with.",
        "parameters": [
          {
            "name": "organization",
            "in": "path",
            "required": true,
            "description": "The name of the Organization the records belong to.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AgentDeclaration"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "A record by this name already existed, and is now what the declaration describes.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Agent"
                }
              }
            }
          },
          "201": {
            "description": "No record by this name existed, and one was made.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Agent"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/Refused"
          },
          "404": {
            "$ref": "#/components/responses/Refused"
          },
          "422": {
            "$ref": "#/components/responses/Refused"
          },
          "503": {
            "$ref": "#/components/responses/Refused"
          }
        }
      }
    },
    "/operator/sessions/{session}/transcript": {
      "get": {
        "operationId": "transcript",
        "summary": "Stream a Session's Transcript.",
        "description": "Replays every entry after `Last-Event-ID`, in order, each as an `entry` event whose id is the cursor that resumes after it. A read that follows then stays open and delivers entries as they are appended; the stream ends with an `end` event once the Session is sealed, or at once for a read that does not follow. A cursor that names no position in this Transcript is refused rather than silently restarting the walk.",
        "parameters": [
          {
            "name": "session",
            "in": "path",
            "required": true,
            "description": "The Session whose Transcript is read.",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "follow",
            "in": "query",
            "required": false,
            "description": "Whether to stay open for entries appended after the ones already recorded.",
            "schema": {
              "type": "boolean",
              "default": true
            }
          },
          {
            "name": "Last-Event-ID",
            "in": "header",
            "required": false,
            "description": "The id of the last entry this reader was delivered. Absent means from the beginning.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The stream is open. Each event carries an entry or the end of the stream as its data, and its kind as its event name.",
            "content": {
              "text/event-stream": {
                "schema": {
                  "$ref": "#/components/schemas/Event"
                }
              }
            }
          },
          "400": {
            "$ref": "#/components/responses/Refused"
          },
          "404": {
            "$ref": "#/components/responses/Refused"
          },
          "503": {
            "$ref": "#/components/responses/Refused"
          }
        }
      }
    }
  },
  "components": {
    "responses": {
      "Refused": {
        "description": "The control plane refused the request.",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/Refusal"
            }
          }
        }
      }
    },
    "schemas": {
      "OrganizationDeclaration": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "name"
        ]
      },
      "Organization": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "name"
        ]
      },
      "WorkspaceDeclaration": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "repositories": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string"
            },
            "description": "The repositories the work happens against, in order."
          },
          "branch": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "name",
          "repositories",
          "branch"
        ]
      },
      "Workspace": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string"
          },
          "repositories": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "branch": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "name",
          "repositories",
          "branch"
        ]
      },
      "AgentDeclaration": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "runtime": {
            "type": "string",
            "minLength": 1,
            "description": "The Agent Runtime that drives it."
          },
          "model": {
            "type": [
              "string",
              "null"
            ],
            "description": "The model it works with. Absent or null names none, and the Agent Runtime's own default is the answer."
          }
        },
        "required": [
          "name",
          "runtime"
        ]
      },
      "Agent": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string"
          },
          "runtime": {
            "type": "string"
          },
          "model": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "id",
          "name",
          "runtime",
          "model"
        ]
      },
      "Recorded": {
        "description": "One Transcript entry, and where it sits in the order.",
        "type": "object",
        "properties": {
          "seq": {
            "type": "integer",
            "format": "int64",
            "minimum": 1
          },
          "appended_at": {
            "type": "string",
            "format": "date-time"
          },
          "entry": {
            "$ref": "#/components/schemas/Entry"
          }
        },
        "required": [
          "seq",
          "appended_at",
          "entry"
        ]
      },
      "Entry": {
        "description": "One entry of a Session's Transcript.",
        "oneOf": [
          {
            "$ref": "#/components/schemas/ParticipantJoined"
          },
          {
            "$ref": "#/components/schemas/Brief"
          },
          {
            "$ref": "#/components/schemas/RunStarted"
          },
          {
            "$ref": "#/components/schemas/Message"
          },
          {
            "$ref": "#/components/schemas/Messages"
          },
          {
            "$ref": "#/components/schemas/RunEnded"
          }
        ],
        "discriminator": {
          "propertyName": "kind",
          "mapping": {
            "participant_joined": "#/components/schemas/ParticipantJoined",
            "brief": "#/components/schemas/Brief",
            "run_started": "#/components/schemas/RunStarted",
            "said": "#/components/schemas/Message",
            "messages": "#/components/schemas/Messages",
            "run_ended": "#/components/schemas/RunEnded"
          }
        }
      },
      "ParticipantJoined": {
        "description": "A Participant joined the Session.",
        "type": "object",
        "properties": {
          "kind": {
            "const": "participant_joined"
          },
          "participant": {
            "type": "string"
          }
        },
        "required": [
          "kind",
          "participant"
        ]
      },
      "Brief": {
        "description": "The instruction a Session started with, rendered by the Trigger that fired.",
        "type": "object",
        "properties": {
          "kind": {
            "const": "brief"
          },
          "trigger": {
            "type": "string"
          },
          "brief": {
            "type": "string"
          }
        },
        "required": [
          "kind",
          "trigger",
          "brief"
        ]
      },
      "RunStarted": {
        "description": "A Run started on behalf of the Session.",
        "type": "object",
        "properties": {
          "kind": {
            "const": "run_started"
          },
          "run": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "kind",
          "run"
        ]
      },
      "Message": {
        "description": "What a Participant said.",
        "type": "object",
        "properties": {
          "kind": {
            "const": "said"
          },
          "participant": {
            "type": "string"
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "kind",
          "participant",
          "message"
        ]
      },
      "Messages": {
        "description": "Messages that arrived while a Run was active, drained as one Transcript entry.",
        "type": "object",
        "properties": {
          "kind": {
            "const": "messages"
          },
          "messages": {
            "type": "array",
            "minItems": 1,
            "items": {
              "$ref": "#/components/schemas/SpokenMessage"
            }
          }
        },
        "required": [
          "kind",
          "messages"
        ]
      },
      "SpokenMessage": {
        "description": "One Participant's message within a drained group.",
        "type": "object",
        "properties": {
          "participant": {
            "type": "string"
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "participant",
          "message"
        ]
      },
      "RunEnded": {
        "description": "A Run ended, and how it went.",
        "type": "object",
        "properties": {
          "kind": {
            "const": "run_ended"
          },
          "run": {
            "type": "string",
            "format": "uuid"
          },
          "exit": {
            "$ref": "#/components/schemas/Exit"
          }
        },
        "required": [
          "kind",
          "run",
          "exit"
        ]
      },
      "Exit": {
        "description": "How a Run ended.",
        "type": "object",
        "properties": {
          "status": {
            "enum": [
              "succeeded",
              "failed"
            ]
          },
          "because": {
            "type": "string",
            "description": "Why it failed. Absent when it succeeded."
          }
        },
        "required": [
          "status"
        ]
      },
      "Refusal": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string"
          }
        },
        "required": [
          "message"
        ]
      },
      "End": {
        "description": "The control plane closed the stream on purpose. A stream that closes without one was cut off, and is resumed from the last id delivered.",
        "type": "object",
        "properties": {
          "because": {
            "enum": [
              "caught_up",
              "sealed"
            ],
            "description": "`caught_up` when a read that does not follow has delivered every entry; `sealed` when the Session is sealed and every entry it will ever hold has been delivered."
          }
        },
        "required": [
          "because"
        ]
      },
      "Event": {
        "description": "One event on the stream. Its event name is its kind.",
        "oneOf": [
          {
            "$ref": "#/components/schemas/Recorded"
          },
          {
            "$ref": "#/components/schemas/End"
          }
        ]
      }
    }
  }
}
